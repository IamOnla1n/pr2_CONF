# Вариант №15

Разработать инструмент визуализации графа зависимостей для менеджера пакетов. Готовые средства (менеджеры пакетов, библиотеки) для получения зависимостей использовать нельзя.

## Этап 1. Минимальный прототип с конфигурацией

**Цель:** создать минимальное CLI-приложение и сделать его настраиваемым.

**Требования:**

1. Источником настраиваемых пользователем параметров являются опции командной строки.
2. К настраиваемым параметрам относятся:
   - Имя анализируемого пакета.
   - URL-адрес репозитория или путь к файлу тестового репозитория.
   - Режим работы с тестовым репозиторием.
   - Версия пакета.
   - Имя сгенерированного файла с изображением графа.
3. (только для этого этапа) При запуске приложения вывести все параметры, настраиваемые пользователем, в формате ключ-значение.
4. Реализовать и продемонстрировать обработку ошибок для всех параметров.
5. Результат выполнения этапа сохранить в репозиторий стандартно оформленным коммитом.
# Описание функций и структур

## Структура CLI приложения

**Назначение:** Парсинг и валидация аргументов командной строки

**Основные компоненты:**

---

## Функции валидации

### `validate_package_name(name)`
**Назначение:** Валидация имени пакета

**Логика:**
- Проверяет, что имя не пустое
- Проверяет допустимые символы: буквы, цифры, `.`, `-`, `_`
- **Возвращает:** очищенное имя пакета
- **Исключения:** `ValueError` при невалидном имени

### `validate_url(url)`
**Назначение:** Валидация URL или пути к файлу

**Логика:**
- Проверяет HTTP/HTTPS URL или существование локального файла
- **Возвращает:** очищенный URL или путь
- **Исключения:** `ValueError` при невалидном URL или отсутствующем файле

### `validate_filename(filename)`
**Назначение:** Валидация имени выходного файла

**Логика:**
- Проверяет поддерживаемые расширения: `.png`, `.jpg`, `.jpeg`, `.svg`, `.pdf`
- Проверяет отсутствие запрещенных символов в имени файла
- **Возвращает:** очищенное имя файла
- **Исключения:** `ValueError` при невалидном имени файла

### `validate_version(version)`
**Назначение:** Валидация версии пакета

**Логика:**
- Проверяет базовый формат версии
- **Возвращает:** очищенную версию
- **Исключения:** `ValueError` при невалидной версии

---

## Основная функция `main()`

**Назначение:** Точка входа в приложение, обработка аргументов командной строки

**Аргументы командной строки:**

- **`--package`** (обязательный) - имя анализируемого пакета
- **`--url`** (обязательный) - URL репозитория или путь к файлу  
- **`--test-mode`** (опциональный) - режим тестового репозитория
- **`--version`** (опциональный) - версия пакета (по умолчанию: 'latest')
- **`--output`** (опциональный) - имя выходного файла (по умолчанию: 'dependency_graph.png')

**Обработка ошибок:**
- **`argparse.ArgumentError`** - ошибки парсинга аргументов
- **`ValueError`** - ошибки валидации параметров
- **`Exception`** - неожиданные ошибки

---

## Запуск тестов


- **Запуск теста с конкретными параметрами**


<img width="1360" height="298" alt="image" src="https://github.com/user-attachments/assets/c73c884b-b025-4b7e-ba96-fe74e1311134" />

- **Запуск теста с ошибкой валидации**


<img width="1274" height="112" alt="image" src="https://github.com/user-attachments/assets/f8934b0d-50f7-4ebc-86de-e33973c3f72d" />


---
# Инструмент визуализации графа зависимостей пакетов Ubuntu

## Этап 2. Сбор данных

**Цель:** реализовать основную логику получения данных о зависимостих для их дальнейшего анализа и визуализации.

**Требования:**

1. Использовать формат пакетов Ubuntu (apt).
2. Информацию необходимо получить для заданной пользователем версии пакета.
3. Извлечь информацию о прямых зависимостих заданного пользователем пакета, используя URL-адрес репозитория.
4. (только для этого этапа) Вывести на экран все прямые зависимости заданного пользователем пакета.
5. Результат выполнения этапа сохранить в репозиторий стандартно оформленным коммитом.

# Описание функций и структур

## Структура CLI приложения

**Назначение:** Парсинг и валидация аргументов командной строки

**Основные компоненты:**

---

## Функции валидации

### `validate_package_name(name)`
**Назначение:** Валидация имени пакета

**Логика:**
- Проверяет, что имя не пустое
- Проверяет допустимые символы: буквы, цифры, `.`, `-`, `_`, `+`
- **Возвращает:** очищенное имя пакета
- **Исключения:** `ValueError` при невалидном имени

### `validate_url(url)`
**Назначение:** Валидация URL репозитория Ubuntu

**Логика:**
- Проверяет HTTP/HTTPS URL официальных репозиториев Ubuntu
- **Возвращает:** очищенный URL
- **Исключения:** `ValueError` при невалидном URL

### `validate_filename(filename)`
**Назначение:** Валидация имени выходного файла

**Логика:**
- Проверяет поддерживаемые расширения: `.png`, `.jpg`, `.jpeg`, `.svg`, `.pdf`
- Проверяет отсутствие запрещенных символов в имени файла
- **Возвращает:** очищенное имя файла
- **Исключения:** `ValueError` при невалидном имени файла

### `validate_version(version)`
**Назначение:** Валидация версии пакета

**Логика:**
- Проверяет базовый формат версии пакетов Ubuntu
- **Возвращает:** очищенную версию
- **Исключения:** `ValueError` при невалидной версии

---

## Класс UbuntuAPTRepository

### `download_packages_index(distribution, component)`
**Назначение:** Загрузка индекса пакетов из репозитория Ubuntu

**Логика:**
- Загружает файл Packages.gz с официального mirror
- Распаковывает и парсит данные в формате Debian
- **Возвращает:** словарь с информацией о пакетах

### `find_package_version(package_name, version, packages_data)`
**Назначение:** Поиск информации о версии пакета

**Логика:**
- Находит пакет в загруженных данных
- Проверяет соответствие версии
- **Возвращает:** информацию о пакете

### `extract_dependencies(package_info)`
**Назначение:** Извлечение прямых зависимостей пакета

**Логика:**
- Анализирует поля Depends и Pre-Depends
- Обрабатывает альтернативы и версии зависимостей
- **Возвращает:** список имен пакетов-зависимостей

---

## Основные функции

### `get_package_dependencies_ubuntu(package_name, version, repo_url, test_mode)`
**Назначение:** Получение зависимостей пакета из репозитория Ubuntu

**Логика:**
- Загружает данные из репозитория
- Находит информацию о пакете
- Извлекает зависимости
- **Возвращает:** список прямых зависимостей

### `main()`
**Назначение:** Точка входа в приложение, обработка аргументов командной строки

**Аргументы командной строки:**

- **`--package`** (обязательный) - имя анализируемого пакета
- **`--url`** (опциональный) - URL репозитория Ubuntu
- **`--test-mode`** (опциональный) - режим тестового репозитория
- **`--version`** (опциональный) - версия пакета (по умолчанию: 'latest')
- **`--output`** (опциональный) - имя выходного файла (по умолчанию: 'dependency_graph.png')

**Обработка ошибок:**
- **`ValueError`** - ошибки валидации параметров
- **`Exception`** - неожиданные ошибки

---
Запуск тестов
# Реальный анализ пакета nginx
python main.py --package nginx --version 1.18.0
<img width="900" height="717" alt="image" src="https://github.com/user-attachments/assets/79667bcc-e60a-42de-8633-32bc5b119797" />


# Анализ python3 с официального репозитория
python main.py --package python3 --url http://archive.ubuntu.com/ubuntu/
<img width="972" height="726" alt="image" src="https://github.com/user-attachments/assets/002d3d35-a70c-4bc4-881a-ed5c841f90bf" />


# Тестовый режим
python main.py --package firefox --test-mode

<img width="656" height="456" alt="image" src="https://github.com/user-attachments/assets/4d28f786-cd31-4208-91e9-81e6700c1fe0" />

---


## Этап 3. Основные операции

**Цель:** построить граф зависимостей (с учетом транзитивности) и выполнить основные операции над ним.

**Требования:**

1. Получение графа зависимостей реализовать алгоритмом BFS с рекурсией.
2. Корректно обработать случаи наличия циклических зависимостей.
3. Поддержать режим тестирования. Вместо URL реального репозитория, дать возможность пользователю указать путь к файлу описания графа репозитория, где пакеты называются большими латинскими буквами. Продемонстрировать функциональность этого этапа на различных случаях работы с тестовым репозиторием.
4. Результат выполнения этапа сохранить в репозиторий стандартно оформленным коммитом.

# Описание функций и структур

## Структура CLI приложения

**Назначение:** Парсинг и валидация аргументов командной строки

**Основные компоненты:**

---

## Функции валидации

### `validate_package_name(name)`
**Назначение:** Валидация имени пакета

**Логика:**
- Проверяет, что имя не пустое
- Проверяет допустимые символы: буквы, цифры, `.`, `-`, `_`, `+`
- **Возвращает:** очищенное имя пакета
- **Исключения:** `ValueError` при невалидном имени

### `validate_url(url)`
**Назначение:** Валидация URL репозитория Ubuntu или пути к тестовому файлу

**Логика:**
- Проверяет HTTP/HTTPS URL или существование локального файла
- **Возвращает:** очищенный URL или путь
- **Исключения:** `ValueError` при невалидном URL или отсутствующем файле

### `validate_filename(filename)`
**Назначение:** Валидация имени выходного файла

**Логика:**
- Проверяет поддерживаемые расширения: `.png`, `.jpg`, `.jpeg`, `.svg`, `.pdf`
- Проверяет отсутствие запрещенных символов в имени файла
- **Возвращает:** очищенное имя файла
- **Исключения:** `ValueError` при невалидном имени файла

### `validate_version(version)`
**Назначение:** Валидация версии пакета

**Логика:**
- Проверяет базовый формат версии пакетов Ubuntu
- **Возвращает:** очищенную версию
- **Исключения:** `ValueError` при невалидной версии

---

## Класс UbuntuAPTRepository

### `download_packages_index(distribution, component)`
**Назначение:** Загрузка индекса пакетов из репозитория Ubuntu

**Логика:**
- Загружает файл Packages.gz с официального mirror
- Распаковывает и парсит данные в формате Debian
- **Возвращает:** словарь с информацией о пакетах

### `extract_dependencies(package_info)`
**Назначение:** Извлечение прямых зависимостей пакета

**Логика:**
- Анализирует поля Depends и Pre-Depends
- Обрабатывает альтернативы и версии зависимостей
- **Возвращает:** список имен пакетов-зависимостей

---

## Класс TestRepository

### `__init__(file_path)`
**Назначение:** Инициализация тестового репозитория

**Логика:**
- Загружает и парсит файл тестового репозитория
- Проверяет, что пакеты названы большими латинскими буквами

### `get_dependencies(package_name)`
**Назначение:** Получение зависимостей пакета из тестового репозитория

**Логика:**
- Возвращает список зависимостей для указанного пакета
- **Возвращает:** список имен пакетов-зависимостей

---

## Основные функции

### `build_dependency_graph_bfs(start_package, get_dependencies_func, max_depth)`
**Назначение:** Построение графа зависимостей алгоритмом BFS с рекурсией

**Логика:**
- Рекурсивно обходит зависимости с помощью BFS
- Обнаруживает и обрабатывает циклические зависимости
- **Возвращает:** граф зависимостей в виде словаря

### `get_dependencies_func_factory(repo_url, test_mode)`
**Назначение:** Фабрика функций для получения зависимостей

**Логика:**
- Создает функцию для получения зависимостей в зависимости от режима
- **Возвращает:** функцию для получения зависимостей пакета

### `get_package_dependencies_ubuntu(package_name, version, repo_url, test_mode)`
**Назначение:** Получение графа зависимостей пакета

**Логика:**
- Координирует процесс построения графа зависимостей
- **Возвращает:** полный граф зависимостей

### `main()`
**Назначение:** Точка входа в приложение, обработка аргументов командной строки

**Аргументы командной строки:**

- **`--package`** (обязательный) - имя анализируемого пакета
- **`--url`** (опциональный) - URL репозитория Ubuntu или путь к файлу тестового репозитория
- **`--test-mode`** (опциональный) - режим работы с тестовым репозиторием
- **`--version`** (опциональный) - версия пакета (по умолчанию: 'latest')
- **`--output`** (опциональный) - имя выходного файла (по умолчанию: 'dependency_graph.png')

**Обработка ошибок:**
- **`ValueError`** - ошибки валидации параметров
- **`Exception`** - неожиданные ошибки

---

### Режим реального репозитория Ubuntu
python main.py --package nginx --url http://archive.ubuntu.com/ubuntu/ --output graph.png


### Тестирование
<img width="898" height="539" alt="image" src="https://github.com/user-attachments/assets/c394ab6e-d324-482c-9456-839113c1aa3b" />
